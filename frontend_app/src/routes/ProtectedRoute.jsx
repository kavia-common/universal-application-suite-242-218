import React from "react";

/**
 * PUBLIC_INTERFACE
 * Placeholder for protected route wrapper.
 */
export default function ProtectedRoute({ children }) {
  // TODO: Integrate with AuthContext and routing to protect pages.
  return <>{children}</>;
}
